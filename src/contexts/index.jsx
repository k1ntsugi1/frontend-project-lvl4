
import { createContext } from "react";

const AuthContext = createContext({});

const SocketContext = createContext({});

const BadWordsContext = createContext({});

export { AuthContext, SocketContext, BadWordsContext };